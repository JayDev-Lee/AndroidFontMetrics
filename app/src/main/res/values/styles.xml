<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="AppTheme.CheckBoxAscent">
        <item name="android:colorControlActivated">@color/ascent</item>
    </style>


    <style name="AppTheme.CheckboxTop">
        <item name="android:colorControlActivated">@color/top</item>
    </style>
    <style name="AppTheme.CheckboxBaseline">
        <item name="android:colorControlActivated">@color/baseline</item>
    </style>
    <style name="AppTheme.CheckboxDescent">
        <item name="android:colorControlActivated">@color/descent</item>
    </style>
    <style name="AppTheme.CheckboxBottom">
        <item name="android:colorControlActivated">@color/bottom</item>
    </style>
    <style name="AppTheme.MeasuerdWidth">
        <item name="android:colorControlActivated">@color/measured_width</item>
    </style>
    <style name="AppTheme.TextBounds">
        <item name="android:colorControlActivated">@color/text_bounds</item>
    </style>

</resources>
